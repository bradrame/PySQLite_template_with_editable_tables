/*
CREATE TABLE IF NOT EXISTS users(
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    name TEXT NOT NULL,
    username TEXT NOT NULL,
    password TEXT NOT NULL
);

INSERT INTO users (name, username, password)
    VALUES ('Brad Ramey', 'bradramey', 'LuNchM3@t');
*/

-- SELECT * FROM users;